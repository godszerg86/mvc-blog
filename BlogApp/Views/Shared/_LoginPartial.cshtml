@using Microsoft.AspNet.Identity
@using BlogApp.Helpers;
@if (Request.IsAuthenticated)
{
    <li>
        @using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
        {
        @Html.AntiForgeryToken()

        if (User.IsInRole("Admin"))
        {
        @*<li>*@ @Html.ActionLink("ADD POST", "Create")@*</li>*@
        }
        @*<li>*@
        @Html.ActionLink("Hello, " + User.Identity.GetUserDisplayName() + " ", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })
        @*</li>*@
        @*<li>*@<a href="javascript:document.getElementById('logoutForm').submit()">Log Out</a>@*</li>*@
        }
    </li>
}
else
{

    <li>@Html.ActionLink("Register", "Register", "Account", new { ReturnUrl = HttpContext.Current.Request.RawUrl }, new { id = "registerLink" })</li>
    <li>@Html.ActionLink("Log in", "Login", "Account", new { ReturnUrl = HttpContext.Current.Request.RawUrl }, new { id = "loginLink" })</li>
}
